{
  "name": "@sw/arcade-engine",
  "version": "0.1.0",
  "description": "Framework-agnostic core for Arcade Arena: pose interpretation, gesture state machines, the audio-clock lookahead scheduler, procedural music bed, cue-bank rotation, effort scoring, and the session orchestrator. Zero React imports — the WebXR starter can mount it too.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./replay": "./src/replay/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@sw/shell": "workspace:*"
  }
}
